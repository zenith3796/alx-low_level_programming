#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main -Determines if a number is positive, negative or zero.
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
int ch = 'a';
int CH = 'A';

while (CH <= 'Z')
{
putchar(CH);
CH++;
}

putchar('\n');
return (0);
}
